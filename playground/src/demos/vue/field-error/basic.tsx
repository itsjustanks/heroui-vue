import { FieldError } from "@itsjustanks/heroui-vue";

/** FieldError basic — React. */
import { defineComponent } from "vue";
export default defineComponent(() => () => <FieldError>Email is required</FieldError>);
