import { ErrorMessage } from "@itsjustanks/heroui-vue";

/** ErrorMessage basic — React. */
import { defineComponent } from "vue";
export default defineComponent(() => () => <ErrorMessage>
      Something went wrong. Please try again.
    </ErrorMessage>);
