import { ErrorMessage } from '@heroui/react'

/** ErrorMessage basic — React. */
export default function ErrorMessageBasic () {
  return (
    <ErrorMessage>
      Something went wrong. Please try again.
    </ErrorMessage>
  )
}
