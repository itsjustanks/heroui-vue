/**
 * Header — faithful Vue port of HeroUI v3 `Header`.
 */
import { HeaderRoot } from './header'

export const Header = Object.assign(HeaderRoot, {
  Root: HeaderRoot
})

export { HeaderRoot }
export { headerVariants } from '@heroui/styles'
