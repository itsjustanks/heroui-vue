/**
 * @deprecated Use `SelectPopover` instead — `SelectContent` was renamed in v1.0
 * to match HeroUI v3 React's `Select.Popover` compound API.
 */
export { SelectPopover as SelectContent } from './select-popover'
export { SelectPopover as default } from './select-popover'
