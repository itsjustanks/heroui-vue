/**
 * HeroUI-Vue Input — faithful HeroUI v3 text-field control over a native input.
 * Part of the HeroUI-for-Vue primitive library (`.planning/prps/heroui-vue-library.md`).
 *
 * Export names mirror `shadcn/input` so call-site migration is a pure
 * import-path swap.
 */
export { default as Input } from './input'
