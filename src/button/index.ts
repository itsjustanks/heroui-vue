/**
 * HeroUI-Vue Button — faithful HeroUI v3 button over reka-ui.
 * Part of the HeroUI-for-Vue primitive library.
 *
 * Compat exports mirror `shadcn-vue` exactly:
 * `Button`, `buttonVariants`, and the `ButtonVariants` type.
 */
export { default as Button } from './button'
export { buttonVariants, type ButtonVariants } from './button-variants'
